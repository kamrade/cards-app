<!DOCTYPE html>
html(lang="en")
head
  meta(charset="UTF-8")
  title Node JS app
  link(rel="stylesheet", href="static/css/styles.css")
body
  h1 Node JS app
  p.desc Sub Heading
  p= title
  ul
    each val in nav
      li
        a(href= val.link)= val.text